# SFPI Compiler Uplift Handoff — CLEAN-MACHINE PICKUP (single self-contained file)

Last updated: 2026-08-17 (post-pull adversarial review — see /home/nkapre/PULL_ANALYSIS-20260817.md for the full 4-review synthesis; 6 CONFIRMED defects, fixes ranked there). Previous wrap 2026-08-17 ~14:15 UTC (merged staging branches deleted from remotes; 7 archive/* branches pushed; local box cleaned 354→~180GB). Assume ZERO local state: no home dir, no worktrees, no toolchains, no evidence dirs. Everything you need is (a) inlined here or (b) on the GitHub remotes at the SHAs in §2. Sections §A (local-only appendix) describe artifacts that exist ONLY on the original machine (nkapre's localdev) — useful if you have it, ignorable if you don't.

## 0. Mission and standing user directives

**Mission:** make ordinary typed C++ compile to code competitive with or faster than handwritten tt-llk LLKs via reusable generic compiler mechanisms; the compiler becomes the performance gold standard for all LLK work. Characterize every op with the 2x2 matrix {semantic source, handwritten source} × {passes OFF, ON}: semantic OFF→ON = causal compiler claim; semantic-ON vs hand = competitiveness claim; never mix them.

**Standing user directives (in force, 2026-08-17 final):**
1. **Work resumes on a FRESH MACHINE.** All agents on the original machine are KILLED (their unpushed local partials are abandoned — everything that matters is on the remotes at §2's SHAs). The three fresh-machine tasks are now DONE and adversarially reviewed: (a) craq-sim cleanup (§6a, tip f80a8d6), (b) WP8 de-calendar hardening (§6b, in sfpi-gcc tip), (c) sfpi-gcc unification (landed in the §10 order: planner → sdpa → profit). Current work = the fix list in PULL_ANALYSIS-20260817.md §4 (P0: all-lanes proof; P1: sweep gate defects + compiler-sha pin promotion).
2. **One coherent branch per repo: nkapre/sfpi.** Agent branches are staging.
3. **No hardcoding in compiler decision logic, ever:** no operation names, exact opcode calendars, coefficient/immediate fingerprints, or magic instruction words. Raw encodings only in target encoding/capability tables and test expectations. Refusals byte-identical. Generic = renamed-equivalent tests + varied constants + near misses + ≥2 unrelated shapes. The same spirit now applies to craq-sim: it must decode/execute macros architecturally from programmed state, never recognize specific shapes.
4. LLK attack lanes (§7) remain PAUSED until the user says resume.

## 1. Protocols (non-negotiable)

**Silicon protocol:** (1) final ELF must change for the intended generic mechanism; (2) OFF/ON identical source/input/golden, only compiler flags differ; (3) paired CRAQ correctness first; (4) independent review of compiler mutations before silicon; (5) serialize ALL device jobs under two exclusive flocks — `flock -x /tmp/tt-device.lock -c "flock -x /tmp/tt-llk-sfpu-silicon.lock -c '<one job>'"` (create the files if absent; release = process exit; never parse results while holding); (6) paired physical correctness PASS, then 3 fresh profiler processes per selector, alternating OFF/ON, unique RUNNER_TEMP per process, raw/post CSVs copied before releasing the lock; (7) archive ELF, .text hash, build.h, logs, compiler sha256, SHA256SUMS manifest. CRAQ = correctness/structure evidence only, NEVER a silicon perf oracle. Don't run hardware on byte-identical OFF/ON pairs except as intentional control. Never force-push; normal pushes only. If a distinct handwritten source exists and its OFF/ON .text is byte-identical, one physical hand run fills both hand cells (record the identity).

**Metric caveat (silicon-learned):** the SDPA BODY profiler marker is INVALID for fire-and-forget replay-launch shapes (a measured 367 violated the 512-slot issue lower bound). For such shapes use the drain-inclusive KERNEL marker, or add a math-drain barrier to the marker zone (open harness follow-up). Sanity-check every reading against the payload's issue-slot lower bound.

**Delivery-cost model:** RISC-pushed op ≈ 1.23× a replayed slot (re-fit from measured actuals; the older 2.2:1 over-predicts launch-conversion gains ~7x).

## 2. Remotes — clone these, everything lives here

| Repo (https://github.com/tenstorrent/…) | nkapre/sfpi | Agent branches (staging, all pushed) |
|---|---|---|
| **sfpi** (wrapper + superproject; BUILDS THE TOOLCHAIN) | tip `bc08700cd1` — gcc pin `bb56f1d77`; SFPI_COMPILER_UPGRADE.md fully reconciled (adversarial pull-analysis in §18.8.0.3, §18.9 B0 marked SATISFIED, historical claims struck+SUPERSEDED); handoff snapshot in docs/handoff-20260817/ | merged staging deleted; no agent branches remain (typed-setrwc deleted after ancestry verification by the other machine) |
| **sfpi-gcc** | `8b62a832a` (bb56f1d77 recalibrated stack + rescued WP7-era oracle provenance store in testsuite/.../oracles/provenance-20260816) | staging branches superseded by the landed merges; `agent/typecast-descriptor-share` @ 405f007c5 content landed |
| **tt-metal** | `9bb5b57594` (69d61d66 corpus stack + typecast blocker doc + ifdef-guarded probe; zero agent branches remain anywhere) | remaining `agent/typecast-descriptor-share` @ f3b5bba3; 7 `archive/*` branches (triaged old-swarm keepers: topk-f1 lineage, corpus-runner-v3, Welford rigs ×3, sigappx-pwl typed source — useful for §7c, softmax audit rig) |
| **craq-sim** | `f80a8d6` (§6a cleanup DONE: all recognizers deleted, generic descriptor-driven execution only; 8/8 Min/Max matrix bit-exact through the generic path; diff-fuzz 1000/1000 bh+wh; verify origin/nkapre/sfpi actually points here) | none |

**Branch contents.** sfpi-gcc `agent/generic-macro-planner`: the 7-layer generic SFPLOADMACRO planner (typed effect attributes w/ refusing defaults → rvtt-effects API → shared path-sensitive ownership → dataflow region discovery → DAG scheduler → capability tables (rvtt-macro-tables*, raw words' legitimate home) → descriptor synthesis + emission-gating verifier), Min/Max exact calendar DELETED and re-derived byte-identically (incl. real in-place-store kernel via a generic store-demoted scheduling fallback), BH launch addr-mode <<13 fix (three-source proven), 0x37120004 magic word deleted (typed `rvtt_ttdstface` insn assembles byte-identically → 0xdc480010). All flags default-off; default codegen proven byte-identical vs e4b974208. sfpi-gcc `agent/sdpa-partial-invariant` (3 independent reviews, APPROVE_WITH_NOTES): partial invariant hoist under LREG pressure → replay-aware complete unroll (+pragma + NULL-latch guards) → region-scoped structural ownership with deferred entry-edge split → dst-autoincr (owned SETC16 + store-mode-6 implicit advance) → M1 final-row→launch conversion → M2a dominating SETC16 placement + distance guard → multi-block (face-loop-scope) hoist + ttdstface port. sfpi-gcc profitability gate (RECALIBRATED at bb56f1d77 from 4 silicon points — the old formula ordered the silicon winner below both losers): benefit = trips×(deliver − max(123, execute)) − deliver, in CENTISLOTS (deliver = (1+len)×123, execute = 100×len), MIN_BENEFIT=60; `-mtt-tensix-replay-hoist-min-benefit=` CHANGED UNITS slots→centislots and default 64→60; proven trips only; Log/Log1p still byte-identical refusals, ReduceSDPA (4,8)=121 and SDPA-exp (8,24)=2325 fire. Verified not-a-curve-fit: 76 unseen probe points match the plane exactly, no notch.

## 3. Silicon scoreboard

**AUTHORITATIVE CLASS IS NOW p150** (tt-metal 69d61d66 baselines, chip-class-separated files, p100a file immutable). p150 re-measurements at 69d61d66: SDPA sem 1289→1036 (**−19.63%** KERNEL) / hand 1009; Min/Max **−30.72%** / +8.26% vs hand; Signbit planner-fired **−22.98%** causal, beats hand −5.81% (the old "must be reproduced by the planner" condition is DISCHARGED); Expm1 −2.49%; Lerp −13.43%; ReduceSDPA generated 834 vs hand 840 (gate-fix promotion pair, bb56f1d77 compiler); open losses persist: Exp +37.61%, SigmoidAppx +63.68%, Typecast WORSENED to +22.64% — attribution RESOLVED by real-node planner dump (2026-08-17 delta review): the ON leg measured a REFUSED replay-fallback build; the planner PLANS the full descriptor (rows=8, verify ok) then refuses emission with config-ownership-unproven; real blocker = blocker-doc spec point 3 (config ownership vs the shared unary init in run_kernel), NOT step-4 absence (closes PULL_ANALYSIS §4 item 7). CAVEATS: Min/Max p150 cells are NOT same-source with the p100a records (65d2c873/4ff5c848 changed the measured kernel's .text — ratio-level reproduction only); whether the p150 SDPA ON bytes equal b0d9e72e is not recorded in-repo; typecast ON leg used a BODY-family marker on a macro-launch shape with no lower-bound check recorded. Raw evidence lives on tt-quietbox-0 (~/sfpi-uplift), not in-repo.

**Historical p100a records below** (3 fresh procs/selector; deterministic ×3; kept for lineage, do not mix arithmetically with p150):

**SDPA Exp Unclamped** (distinct hand LLK exists; hand OFF==ON byte-identical `45accd4b…`/1392B):

| baseline | sem OFF | sem ON | causal | hand | vs hand |
|---|---:|---:|---:|---:|---:|
| original source, BODY marker | 1048 | 715 | **−31.77%** | 632 | +13.13% |
| migrated infra (typed face advance in shared LLK header), KERNEL marker | 1299 | 1057 | **−18.63%** | 1018 | +3.83% |

Progression (same unchanged kernel source): 1048 → 945 (replay-hoist) → 745 (scoped ownership) → 725 (dst-autoincr) → 715 (launch conversion). ON structure at best: 16-slot payload, zero SFPLOADI in capture, zero per-row TTINCRWC, no scalar backedge; with the multi-block hoist, the whole payload+8 loadi+SETC16 trio sits once per tile and the face loop is 8 launches + typed advance. Residual vs hand is chain-depth/drain-bound (per-face-restart hypothesis silicon-refuted). [NOTE superseded: Lane D measured the SDPA 2x2 on p150, see the p150 block above; b0d9e72e byte-identity of the measured ON leg unrecorded.]

**Min/Max** (hand exists; OFF==ON identical): sem OFF 226.65 → sem ON (`-mtt-tensix-macro-planner`) **156.99 = −30.73%**; hand 144.02 → **+9.0% vs hand** (previously booked +41% open loss). First physical proof of derived (non-hardcoded) SFPLOADMACRO emission.

**Semantic-only ops (no distinct hand LLK exists — 2x2 collapses):** Lerp −2.75% (landed win); Log +1.81% REGRESSION and Log1p +2.30% REGRESSION (both now byte-identical refusals under the profitability gate); Expm1 exactly 0.000% (latency-schedule: BH dynamic stalls absorb single-slot static reorders).

**Open different-source losses (STALE-PENDING — hand number exists, no fresh same-source 2x2):** TTNN Where +96.2% (its exact-calendar emitter emitted misc 0x706 vs the sim's 0x770 — semantically different protocols; failed integration correctness; closes only via the planner's CC-template extension, NOT by patching); SigmoidAppx +62.33% (361.80 vs hand 222.88; needs semantic LUT lowering, §7c); Exp semantic +70.7% (989.75 vs 579.74, §7b); Typecast +17.2% macro / +19.6% semantic (313/317 vs 267/265; needs §6b step 4 four-region descriptor sharing); TopK +5.4% (runtime loop/control delivery vs static expansion; zero SFPMOV in both). Addcmul: parity (292.99 vs 292.93, single paired run). Signbit: −7.48% generated win but via the old exact calendar — must be reproduced by the planner (WP8 step 2) before it counts.

**Scout sweep (changed-binary + CRAQ status, five-flag discriminator):** Log/Log1p/Tanh/Gelu/GeluTanh/Mish/Selu/Cbrt = replay-hoist record-only preheader shapes, all CRAQ PASS/PASS, all silicon-withheld (losing shape class; gate now refuses → byte-identical). Expm1 latency-schedule-only (measured 0.000%). Byte-identical OFF/ON: SnakeBeta, Erf, Erfc, Softplus, I0, Erfinv (only Float32→Float32 builds on BH), Addcdiv, Softmax-k, quant/requant/dequant (QSR refuses five flags).

## 4. Eradication ledger

DONE: 0x37120004 magic-word recognition deleted from the compiler (typed insn byte-identical); Min/Max exact calendar deleted at WP7, planner re-derives emission byte-identically (8/8 CRAQ oracle digests EXACT on the real kernel); stale <<14 in GCC-vendored sfpu-ops-bh.h fixed to proven <<13; function-global opacity gates replaced by region-scoped structural ownership on both stacks; §6b DONE — Signbit/cast-round/Typecast/Where calendars deleted, rtl-rvtt-loadmacro.cc deleted entirely (5f31e00f0) after oracle mint (32e20f9fd), mtt-tensix-{analyze,emit}-loadmacro flags error on use; §6a DONE — all craq-sim recognizers deleted (f80a8d6), no renamed re-introduction. REMAINS: raw words in capability/encoding tables + test expectations (by design); gcc table comments still cite line numbers of the deleted rtl-rvtt-loadmacro.cc (documentation, acceptable).

## 5. Review carry-forwards (open risks)

1. Seven capability-table constants flagged architecturally-unproven in the planner branch's NOTES (§9 of NOTES-wp6-prep; <<13 conflict RESOLVED, rest refuse-by-default) — need an independent hardware reference.
2. M1 exit-block non-consumer exemption = accepted risk (revisit if call-saved LREG semantics ever appear); refuse launch-conversion across non-replay-neutral asm once typed effects generalize.
3. M2a loop profitability uses a profile estimate for dynamic rows — unify with the proven-trips discipline.
4. SETC16-to-consume distance guard (=2, cost table) conservative but hardware-unconfirmed — required before explicit-row (non-replay) autoincr shapes hit silicon.
5. [Superseded at bb56f1d77] Four silicon points now exist (ReduceSDPA WIN, SDPA-exp WIN, Log/Log1p LOSS); the old model was refuted and replaced (§2 formula). NEW no-silicon band: modeled benefit [60,121) centislots — incl. the test-pinned (4,9)=90 fire — has zero silicon points; silicon A/B remains the promotion backstop. Also restore rvtt-cost.md's dropped non-itemized-dynamic-costs sentence.
6. (NEW, PROCESS) tt-metal union merges ran with NO compile gate: 9d2d9530 lost a closing brace (all 8 int-fresh compile-producer variants fail at that commit) and resolved a same-hunk semantic conflict in-commit; damage fully repaired at c9e13bc0 and proven nothing-else-lost (three-way block check + 12/12+8/8 compile gates) — mandate a compile-producer gate for tt-metal merges. Also: bd5aab24 AddInt/SubInt 2x2 record verified protocol-compliant and honest.
6b. BODY-marker drain barrier (harness) before ranking launch-dominated shapes — STILL OPEN, and a BODY marker was already used for the p150 typecast macro-launch ON leg with no issue-slot lower-bound check recorded.
7. (P0 — FIXED at 6294a1ba3, independently VERIFIED SOUND by the 2026-08-17 delta review: defect reproduced on stock, every unproved enable refuses byte-identically on fixed, proof derives from the SFPENCC encoding via capability tables, 281/281 corpus identity, oracles reproduce. Residual findings: cc-enable oracle manifest was minted from a dirty tree at 8b62a832a — re-mint at a clean tip + add a --dirty marker; PRE-EXISTING FE operand-role swap at gimple-rvtt-check.cc:164 means user source cannot spell true all-lanes and any FE fix would silently widen the reachable word set — carry-forward; recalibration evidence doc has truncated hashes and no in-repo lock logs.) Original finding, retained for history: All-lanes-enable proof weakened: planner accepts ANY pure CC write as ambient enable (rtl-rvtt-macro-planner.cc:216-266); lanes-off SFPENCC(0,10) FORMS the frozen calendar (confirmed on tip build); sfpencc_all_lanes_word() is dead code. Wire the consumer-side proof or CRAQ-prove the partial-lane envelope BEFORE lane-predicated shapes (typecast faces, Where successors) hit silicon; fix the overstating comment at rvtt-macro-ownership.cc:98-102.
8. (2026-08-17 smell-hunt wave 4, both lanes SMELLS_FOUND / ZERO violations) CONFIRMED GOOD: smag_to_int lowering is charter-clean wrapper SOURCE (no compiler recognizer; generic renamed/varied tests 27/27; self-inverse SFPCAST verified algebraically + against mainline craq tensix.cpp:10572; WH/QSR fallbacks intact); a2c3cad26 merge is an exact mechanical union with the P0 stack intact (706/706); all four sweep-gate D-fixes (D2/D3/D4/D6) verified REAL by execution; no wrong-pin sweep cells in the pin-churn window; claims separation honored. OPEN SMELLS: (a) −34.8% vs-hand cells divide new-pin sem cells by STALE-pin hand cells (33221397), no committed hand .text bridge across the compiler change — commit the bridge or re-run hand; (b) **the −12.7% beat-hand row is stale-pin AND nightly-20260816b flagged a causal WIN→LOSS FLIP under the current pin (sem_on 23.84→24.33, triage pending) — do not cite −12.7%**; (c) ADD_SUB_INT_GENERATED_AB.md headline still says LOSS vs corpus −34.8% win rows (doc/data contradiction); (d) 45c12df2 pin promotion has no in-repo review record; conf admits Reduce-SDPA byte-identity NOT re-proven for the new build; (e) CRAQ sim oracle UNPINNED — any env-supplied libttsim.so satisfies the D3 gate (pin sim sha like cc1plus); (f) --skip-craq-gate leaves NO taint marker in evidence dirs; (g) pin checks opt-in on direct invocation (argparse not enforced; docstring example is unpinned); (h) ALLOW_PIN_OVERRIDE plain env var honored without evidence persistence; (i) issue-slot lower bound opt-in per row, not structural; (j) cc-enable manifest hashes not machine-portable (.ident baked in — strip on re-mint); (k) smag/nightly evidence off-repo only (tt-quietbox-0 ~/sfpi-uplift).
11. (2026-08-18 wave 7 — 49 gcc commits incl. WP11-13/win lanes, Where adjudication, default-ON arc, craq fix; ALL THREE LANES VIOLATIONS_FOUND, 4th consecutive wave)
   THE BIG POSITIVE — SILICON CAUGHT REAL WRONG-CODE AND THE PROCESS HELD: the planner had formed 0x706-class separator-kept where-calendars that deterministically MIS-SELECT on silicon while CRAQ passed them (sim launch-latch bug — the exact sim-blindness our wave-6 review noted as a smell but cleared as confirmed-good: **our own review was falsified; sim-blindness findings get violation weight henceforth**). The adjudication honestly convicted, REFUSED to book the correctness-RED formed number, and craq 9f32414 fixed the sim afterward (spec-consistent, generic, load-bearing tests proven both directions on cold builds; within the relaxed authorization scope). The where re-baseline is the cleanest booking of the saga (all 4 cells, one nightly, one pin: sem 312.5→207.17 = −33.71% causal; hand 159.17 = +30.16% vs-hand). Also verified good on cold builds: default-ON engineering equivalence, FAIL-set frozen from-nothing, pin-chain DAG fully consistent (10 advances exact), raw-word epoch closure is structural not recognition, MOP derivation spec-provenance clean, ICE→named-error legitimate, sigmoid verdicts refuse to book algorithm choice as compiler wins, B5/Track-B and typecast probe (+3.3% vs hand) bookings.
   VIOLATIONS: (V1, 4th wave) baseline header now FIVE pins stale, edited 4 more times, falsified triage prose persists; **(V-NEW, CORRECTNESS-GRADE) the conf's only operative sim-provenance line still names the PRE-FIX silicon-refuted sim f80a8d64 — pin-7 corrected it in prose, pin-8 REPLACED that block and dropped the correction; a fresh operator following the conf builds the OLD sim which green-lights the known-wrong 0x706-class race shapes. Combined with the still-unpinned SIM_BH/SIM_WH (8(e)), this is now a correctness hole, not a provenance nit**; (§1(4) instances 5-6) pins 7/8 promoted → silicon 33-42 min later, zero review records, REVIEW_RECORD enforcement STILL not implemented after being recommended in wave 6; **(default-ON process) 07ab27f0c flipped compiler Init() defaults for dst-ownership/lut-select/setexp-fold — ending the every-flag-default-off invariant — with no independent review and no recorded user authorization, citing as its silicon basis exactly the numbers wave 6 had flagged hours earlier**; (V4) marker-validity defect extended to the new −33.71% where headline (BODY-family marker, empty issue_slot_lb) while unarymaxmin remains unfixed; (V5) merge 63d65e1bc committed literal conflict markers to a testsuite file (fixed 12 min later, pre-pin).
   SMELLS: staged sim shas 32489dda/8f0079a9 cited inside compiler-source provenance comments are NOT OBJECTS in any reachable craq repo (gates ran on unpushed sim states); **flags-off byte-identity is NO LONGER UNIVERSAL — measured: 2/450 shapes change at pure defaults from an unconditional companion-pairing soundness refusal (disclosed in-commit)**; the OFF-leg spelling is now compiler-version-dependent (sweep uses 8 -mno- flags; the historical 5-flag OFF silently includes the promoted trio on new compilers — §10 below corrected); baseline schema corruption (9 rows with a sha in the expected_class enum column — validator doesn't catch it); where headline cells lack the deterministic-x3 statement; adjudication is self-adjudication (Lane AD's own output; VERDICT.md off-box); diff-fuzz not wired into craq ci.sh; replay in sweep-ON vs compiler-default-off drift persists (S9); all wave evidence off-box (8(k)).
   STANDING CORRECTIONS APPLIED to this file: §10 OFF set is now EIGHT -mno- flags on post-promotion compilers (add -mno-tt-tensix-optimize-dst-ownership, -mno-tt-tensix-lut-select?, -mno-tt-tensix-setexp-fold — exact spellings per riscv.opt at aee50405a); §2 'all flags default-off' is FALSE as of 07ab27f0c for the promoted trio. ENFORCEMENT (unchanged, now 4-wave-overdue): REVIEW_RECORD preflight, conf-lint job, issue_slot_lb required, SIM SHA PIN — the last one is now correctness-critical.
10. (2026-08-17 wave 6 — the ~36-commit overnight research wave; ALL THREE LANES VIOLATIONS_FOUND) **The compiler engineering keeps passing hostile review; the measurement/review discipline is now in systemic breach.**
   CONFIRMED GOOD (major): the timing "established facts" rest on a GENUINE independent architectural reference — craq-sim/tests/aristotle/mega-union/specs/SFPLOADMACRO.md (BlackholeA0 ISA functional spec, in-tree since May) normatively contains the SequenceBits format, per-sub-unit legality, Misc/UnitDelayKind layout — closing ledger §5.1 for §9(b)/(c)/(g); the derive-core is real derivation (77/77+542/542+17/17+11/11 reproduced; every frozen calendar word re-derives bit-exactly); WP10 emits the handwritten 0x770 protocol, NOT the failed 0x706; Track B dst-ownership audited line-by-line against its own design risk list — clean, erfinv reload FOLDS with bit-exact paired CRAQ, gelu correctly refuses; sigmoid impl-2 is charter-clean typed C++; both pin promotions honored the conf prose rule; replay admission genuinely excluded from ON sets.
   VIOLATIONS (3rd consecutive wave): (V1) **baseline header STILL lying — now 3 pins stale** (header claims pin-3 CURRENT; conf at pin-6 a45dbca5; edited 4× in this window without repair) AND a77bc22d72 falsifies its own file (overwrote the exact sem_on cells its header says "remain the reviewed record" pending a triage that was resolved at pin-4 and never recorded there); (V2) **§1(4) breach ×2 more (instances 3 and 4)**: silicon ran ~30 min after each unreviewed pin promotion; "Lane AA GO" is the wave's own self-validation, not review; the pin-6 build contains c3c94abab which itself ends "No silicon run (review first)" yet was silicon-measured the same night, and TIMING_CALENDAR_DERIVATION.md §6 forbade silicon this increment; (V3) **the −44.8% unarymaxmin headline has zero recorded validity evidence by the repo's own rule** — sem-ON now forms a fire-and-forget derived macro but ops.tsv keeps marker=TILE_LOOP with issue_slot_lb EMPTY (the header mandates KERNEL for such shapes; _issue_slot_check silently no-ops on empty lb; ledger 6b/8(i) now load-bearing on a headline).
   SMELLS: all three headlines are mixed-generation in the committed record (−44.8% = pin-6 sem ÷ pin-1 hand; −10.9% addcmul = pin-5 sem ÷ compiler-unrecorded hand, with the corpus row STILL saying "parity/planner refuses" — fresh 8(c)-class contradiction; +8.9% sigmoid-tree = pin-6 ÷ pin-5), mitigated only by the sweep computing within-run ratios whose evidence is off-box; 0x770 is ADOPTED (handwritten provenance) not derived, its kind-nibble contradicts the design's own dual-model policy, and the sim is structurally blind to exactly those bits; **the WP10 lane proof is LOAD-BEARING on the pre-existing FE operand-role swap (gimple-rvtt-check.cc:164) — an innocent FE fix would silently open a semantics-changing enable hoist; this carry-forward is now a soundness landmine, not a curiosity**; replay admission is wholesale-per-flag despite its per-A/B comment; dst-ownership effect_overrides[] shadows the attribute family (drift surface, acknowledged in-file); 9(i) ReduceSDPA full-ON refusal still undisclosed and its byte-identity prose was dropped at pin-5; all wave evidence off-box; sim still unpinned (8(e)).
   ENFORCEMENT RECOMMENDATION: implement CRAQ_SFPI.md's structural fixes NOW, before the pivot — REVIEW_RECORD required by sweep preflight (makes §1(4) unbypassable), the conf-lint CI job (mechanizes the must-agree header rule), issue_slot_lb required for macro-launch rows, sim sha pin. The violations are all one missing enforcement layer, repeated.
9. (2026-08-17 smell-hunt wave 5 — bd2e5e876 saturation term + 8c1a9af0/dcfaf407) **FIRST VIOLATION VERDICT (corpus lane): the baseline-header audit trail is now LYING** — sfpu_device_baseline_p150_v1.tsv header still labels pin-3 afe05a86 as the CURRENT conf pin (conf is at pin-4 9827aaac) and dcfaf407 edited that same file while rendering its stale-pin sentence factually false; the header's own must-agree rule is broken. MODEL LANE (zero violations, smells): the saturation term itself is GENERIC and honest (regime shift not island — 226 decisions across ~65 unseen shapes match closed form with 0 inconsistencies; unarymaxmin sits deep in a smooth refuse region; calibration signs preserved; the flip fix is an honest REFUSAL byte-restoring the archived silicon-winning shape). BUT: **(i) UNDISCLOSED winner-class change — ReduceSDPA's shape now REFUSES under the standard full-ON leg (previously fired +121); the measured win exists only at gate-leg flags (autoincr OFF); the new term's 'absorption relieves nothing' prediction has ZERO silicon points — disclose + silicon-backstop before trusting full-ON on that class; (ii) PROTOCOL §1(4) BREACH: nightly-20260816c ran silicon under compiler mutation bd2e5e876 promoted 12 minutes after commit with NO independent review record (in-repo or anywhere reachable); (iii) 8(a) WORSENED — the −34.8% cells now straddle TWO compiler generations (new sem / stale hand), the promised hand re-measure skipped by two consecutive nightlies, and pin-history prose asserts 'confirmed'; (iv) 8c1a9af0 DELETED the conf's honest admission that Reduce-SDPA byte-identity was not re-proven, replacing it with an unproven off-repo claim; (v) selective refresh — only the 4 cells supporting the −34.8% headline were refreshed; (vi) 'all other decisions and bytes proven unchanged' overclaims (~5 families spot-checked, all off-repo); (vii) −12.7% do-not-cite STANDS (conf says resolved, header says triage pending, rows unretracted, reversion hash 03b4e663 uncommitted prose); (viii) flag-coupled discount: autoincr-flag-conditioned separator discount can over-refuse delivery-bound hoists when the pass later refuses (extends item 3); (ix) 8(c)/(e)/(k) unchanged-or-worse.**
8. (NEW) loop_trip_weight (0ee353061) extends the profile-count-estimate approach item 3 wanted unified with proven-trips; documented profitability-only (never a correctness input) — perf risk, not soundness.

## 6a. craq-sim cleanup — DONE at f80a8d6 (adversarially verified 2026-08-17)

All recognizers deleted (6c072b5; provenance list byte-identical to the nine May-2026 shas), no renamed re-introduction, generic descriptor-driven decode only (templates/sequence/misc purely from SFPCONFIG-written state, events retire at issue+1+Delay through the ordinary per-instruction executors). aabbd10c (SFPSTORE mod0=9 LO16) retained. All validation gates re-verified independently: 8/8 Min/Max matrix bit-exact vs frozen oracle through the generic path (TTSIM_TRACE_LOADMACRO shows 1024 launches/test, 0 on OFF), adversarial never-whitelisted descriptor bit-exact, diff-fuzz 1000/1000 bh+wh, sim CI green, non-macro default unaffected. Deliberate documented deviations (fine): absolute scheduling instead of the optional 4-sub-unit FIFO (mul_int32 relies on event passing); delayed-event model now always-on by design. TRACKED (no fix required): execute_load_macro_template_direct (tensix.cpp:11250–~11660) is a ~450-line opcode-generic parallel evaluator for the two non-encodable overrides (LReg16 target; SFPSHFT2 VB←VD) — a divergence-risk surface pinned only by fuzz, with a vd_to_16 coverage ceiling (unsupported opcodes refuse precisely). Verify origin/nkapre/sfpi points at f80a8d6.

## 6b. WP8 de-calendar hardening — DONE at sfpi-gcc tip bb56f1d77, pending this review's fixes

All 5 steps landed and verified end-to-end with an independent tip build: discovery extensions (0ee353061), signbit + cast-round byte-parity (oracles MINTED at 32e20f9fd BEFORE the quarantined pass was deleted at 5f31e00f0; re-minting reproduces every committed hash; signbit formation is structural, no transplanted calendar, shifts form-or-refuse by encodability), typecast four-region descriptor sharing (50cad63fa), Where → named `cc-template-unsupported` refusals, rtl-rvtt-loadmacro.cc + flags deleted (error on use, verified). Minmax parities: 6/6 in-place .frozen + 26/26 refusal-store entries reproduce at tip. docs/MACRO_PLANNER.md committed (cd0af49be). Genericity/refusal obligations present and passing (renamed+varied twins, zero-trip, config-mutation, QSR).
PENDING FIXES from the review: (a) P0 all-lanes proof (§5 item 7); (b) the WP7 minmax parity manifests (19 distinct-address + 6 in-place) + refusal-oracle store still exist ONLY in /localdev/nkapre/minmax-refusal-identity/ — commit them as testsuite support data (only the WP8 manifest oracles/wp8-oracle-manifest.txt was committed); (c) archive/link the bb56f1d77 p150 recalibration A/B evidence + independent-review record (currently only on tt-quietbox-0, device class silently moved p100a→p150).

## 7. PAUSED lane briefs (full specs — resume only on user go-ahead; old-machine worktrees were local-only, so re-execute from the pushed branches)

**(a) Track B — DST/RWC ownership pass.** Authoritative executable design: SFPI_COMPILER_UPGRADE.md §18.9 at sfpi nkapre/sfpi (in-repo). CRITICAL reconciliation: §18.9 was written against the old pin and claims its B0 prerequisite (typed ttdstface/ttsetrwc builtins + wrappers) is greenfield — B0 IS SATISFIED: builtins at sfpi-gcc tip bb56f1d77, wrappers on sfpi, LLK migration on tt-metal nkapre/sfpi. The doc fix LANDED (supersession block at SFPI_COMPILER_UPGRADE.md:2032) but still cites the stale pin ddf44ed64 and leaves the GREENFIELD body text unedited — touch up per §8 item 6. Then per §18.9.3–18.9.5: new pre-IRA pass_rvtt_dst_ownership in rtl-rvtt-dst-ownership.cc mirroring rtl-rvtt-lreg-livein.cc (sentinel pseudos, forward union-join, fresh local join tokens), tracking live Dst faces (dst_row_valid on HI rows, ZEROACC = death), RWC boundaries (ADDR_MOD DestClear/CR/CToCR, SETRWC/INCRWC dst leg, ZEROACC), and a per-face layout tag (fp32/bf16/int8 from reaching ALU_ACC_CTRL_*/dst_32bit_addr_en); lossy join → dst-rwc-effect-unproved refusal; QSR hard-refuses. Milestones: B1 count the author Dst reloads in ckernel_sfpu_{log,erfinv,gelu}.h (log :53/54, erfinv :54, gelu :373); B2 sentinel skeleton (stream byte-identical); B3 liveness/boundaries/layout; B4 cancel proven-identity store/reload pairs — gate: bit-exact CRAQ vs pre-optimization build + reloads gone + refusals byte-identical; B5 silicon non-inferiority (paired, reviewed).
**(b) Exp semantic (+70.7%, biggest loss).** Locate the semantic Exp kernel + its test nodes in tt-metal; compile OFF/ON with the full sdpa-branch flag set (§10); diagnose per-mechanism fired/refused from dumps; payload analysis vs hand Exp (falsifiable-target method: slots, SFPLOADI-in-capture, TTINCRWC, backedges, per-face vs per-tile placement); paired CRAQ; sober 1.23:1 prediction; report ordered generic next steps. This op HAS a hand variant → full 2x2 on silicon after review.
**(c) SigmoidAppx (+62.3%).** Hand code uses SFPLUT/SFPLUTFP32; the semantic source's range-dispatch tree compiles poorly. Generic mechanism: LUT instruction selection keyed to DATAFLOW SHAPE (select-tree/switch over range-partitioned coefficients feeding MAD) + target capability tables — NEVER coefficient values (renamed tests with DIFFERENT coefficients must prove value-independence); possibly + unroll/constant-register allocation. Implement first increment on a branch off the sdpa stack; changed-binary + CRAQ; no silicon without review.

## 8. Next actions (ordered; full ranked list with anchors in PULL_ANALYSIS-20260817.md §4)

1. **P0:** wire the all-lanes-enable proof (rtl-rvtt-macro-planner.cc:216-266; consume sfpencc_all_lanes_word() or CRAQ-prove the partial-lane envelope; tests both directions) BEFORE lane-predicated shapes reach silicon.
2. **P1 (before the next scheduled sweep is trusted):** fix weekly_bh_sweep.sh FAIL counting (grep -c RED-on-clean bug); move knob_silicon() behind the BH CRAQ gate + paired correctness; make win→refusal RED in report(); promote sweep_2x2.conf PINNED_COMPILER_SHA256 to the bb56f1d77-built compiler and add per-row compiler_sha to baseline/scoreboard schema.
3. **P2:** reword 65d2c873/p100a-"CONFIRMED" annotations to ratio-level reproduction (kernel .text changed, verified); run the planner dump on the real metal__ckernel_sfpu_typecast BH node to attribute the +22.64% (fired vs refused) and fix the "WP8 step 4 targets it" phrasing; implement the issue-slot lower-bound check + drain barrier/KERNEL leg for BODY-marker macro-launch rows; add a device chip-class probe to preflight + chip_class in scoreboard.tsv; commit the WP7 manifests + refusal store; archive the recalibration A/B evidence.
4. Then M3 = programmable-constant-register allocation + drain-aware scheduling toward beating hand SDPA; verify/record whether the measured p150 SDPA ON bytes equal b0d9e72e.
5. Resume §7 lanes on user go-ahead.
6. Doc touch-ups: SFPI_COMPILER_UPGRADE.md B0 block cites stale pin ddf44ed64 (→ bb56f1d77) and the superseded GREENFIELD lines in §18.9.2/§18.9.6 remain unedited; re-record the [60,121) no-silicon band + restore the rvtt-cost.md dynamic-costs sentence.

## 9. Fresh-machine bootstrap (from nothing)

```bash
# 1. Toolchain (the superproject builds it)
git clone https://github.com/tenstorrent/sfpi && cd sfpi
git checkout nkapre/sfpi            # tip 9a555cb89f; gcc pin bb56f1d77 (verified exact)
git submodule update --init --recursive   # gcc lands at bb56f1d77 (the pin)
scripts/build.sh                    # cc1plus contains the unified planner+sdpa+profit stacks (all new flags default-off)
# result: sfpi/compiler/bin/riscv-tt-elf-{g++,objdump,...} + sfpi/include (wrappers incl. dst_face_advance)

# 2. Kernels + harness
git clone https://github.com/tenstorrent/tt-metal && cd tt-metal
git checkout nkapre/sfpi            # 69d61d66
ln -s <abs-path-to-built-sfpi-repo> tt_metal/tt-llk/tests/sfpi   # harness compiler hookup (untracked symlink)

# 3. CRAQ simulator (generic descriptor path; §6a cleanup landed — modifiable again under directive 3's sim extension)
git clone https://github.com/tenstorrent/craq-sim && cd craq-sim
git checkout nkapre/sfpi            # f80a8d6; build the release_bh config per its README → libttsim.so

# 4. Python: always python3. Harness dir: tt-metal/tt_metal/tt-llk/tests/python_tests
```

## 10. Harness recipes (inlined; these are the exact working invocations)

**Env per run:** `cd tt-metal/tt_metal/tt-llk/tests/python_tests; export CHIP_ARCH=blackhole LLK_HOME=<tt-metal>/tt_metal/tt-llk; RUNNER_TEMP=<unique-dir-per-process>` (selects the build tree, one per selector/process). Compiler flags ride `TT_LLK_EXTRA_COMPILER_OPTIONS`.

**Flag sets:** OFF = `-mno-tt-tensix-optimize-latency-schedule -mno-tt-tensix-optimize-dst-iteration-fusion -mno-tt-tensix-optimize-replay-hoist -mno-tt-tensix-optimize-invariant-loadi -mno-tt-tensix-optimize-dst-autoincr`. ON = positive spellings of the same. Planner ON leg = `-mtt-tensix-macro-planner`. The loadmacro flags (`-m[no-]tt-tensix-{analyze,emit}-loadmacro`) are DELETED post-WP8 and ERROR on use at the tip compiler — never include them (sweep_2x2.py already dropped them). `-mtt-tensix-replay-hoist-min-benefit=` is now in CENTISLOTS, default 60.

**CRAQ run:** add `TT_METAL_SIMULATOR=<path>/libttsim.so` and `pytest --run-simulator "<node>" -v`.

**Known-good correctness nodes:** `test_sfpu_unary.py::test_eltwise_unary_sfpu[formats:Float16_b->Float16_b-approx_mode:No-mathop:<Op>-fast_mode:No-dest_acc:No-input_dimensions:[64, 64]]` (Log1p/Log/Expm1/...); `test_sfpu_binary.py::test_fresh_cpp_binary_max_min[formats:Float16_b->Float16_b-mathop:SfpuBinary{Max,Min}-dest_acc:No-fresh_cpp_impl:{1=semantic,0=hand}]`; `test_sfpu_sdpa_exp_unclamped.py::test_sfpu_sdpa_exp_unclamped[formats:Float16_b->Float16_b-input_range:(-20.0, 0.0)-scale_en:True-scale:16256-num_tiles:1]`. Discover more: `pytest --collect-only -q | grep -i <op>`.

**Perf nodes/metric:** `perf_eltwise_unary_sfpu.py::test_perf_eltwise_unary_sfpu[...mathop:<Op>-...loop_factor:16-iterations:32-...input_dimensions:[128, 64]]`, `perf_eltwise_binary_sfpu.py::test_perf_fresh_cpp_binary_max_min[...]`, `test_sfpu_sdpa_exp_unclamped_device_profile[1-semantic_unclamped]`. Metric = post CSV, TILE_LOOP (or op BODY) row, mean(MATH_ISOLATE), cycles/tile. CSVs land under `$LLK_HOME/perf_data/<suite>/` and are OVERWRITTEN per run — copy raw+post in-lock immediately after each pytest exits.

**Silicon A/B template (per §1):** verify recompiled .text hashes first; paired CRAQ; then per device job: `flock -x /tmp/tt-device.lock -c "flock -x /tmp/tt-llk-sfpu-silicon.lock -c 'RUNNER_TEMP=... TT_LLK_EXTRA_COMPILER_OPTIONS=\"...\" pytest \"<node>\" -v 2>&1 | tee <log>; cp <csvs> <evidence>'"` — correctness OFF, correctness ON, then r1..r3 alternating OFF/ON perf.

**ELF analysis:** `tt-metal/tools/disassemble.sh --symbol run_kernel --match 'sfploadi|ttreplay|ttincrwc|sfpmad|bne' --context 2 <math.elf>`. BH inserts dependency stalls dynamically — absence of literal SFPNOP proves nothing; use dependencies + silicon timing.

**DejaGnu (never clobber a build tree's evidentiary .sum):** copy `<build>/gcc/testsuite/g++/site.exp` to a scratch dir, append `set GXX_UNDER_TEST "<build>/gcc/xg++ -B<build>/gcc/"`, then `runtest --tool g++ --srcdir <src>/gcc/testsuite 'rvtt.exp=<pattern>'`. Focused families on the branches: loadmacro*, macro-planner*, invariant-loadi*, invariant-replay-unroll*, dst-autoincr*, replay-launch-convert*, ttdstface*, effects-dump*. Full `rvtt.exp` has 15 known pre-existing environmental FAILs (planner branch) / 15-16 (other stacks) — the FAIL SET must stay byte-identical; compare sets, not counts.

**Unification merge gates (sfpi-gcc) — EXECUTED, landed at bb56f1d77 (merges verified mechanically faithful; kept as the template for any future merge):** merge order planner → sdpa → profit into a branch off nkapre/sfpi; after each merge build cc1plus and run ALL focused families present; FAIL set = union of the branches' known pre-existing sets, nothing new; semantic-union conflict resolution (keep both features); STOP and report any same-hunk semantic conflict rather than improvising; push as nkapre/sfpi only when green.

## §A. Local-only appendix (original machine /localdev/nkapre — raw data, NOT required to continue)

Evidence dirs (each with SHA256SUMS): ttnn-op-scout2-20260816 (scouts + Log/Log1p/Expm1 silicon), sdpa-gap-{,scoped-,incrwc-,m1m2a-,m2b-,m2b2-landing-}evidence-20260816 (SDPA lineage incl. residual-93-analysis.md), minmax-silicon-20260817, planner-craq{,2}-evidence-20260816, wp1-trio-evidence-20260816 (verbatim CRAQ matrix runner), replay-profit-evidence-20260816, macro-planner-design-20260816 (original DESIGN.md + audit JSONs — commit into the repo per §6), minmax-refusal-identity (oracle store — commit per §6), audit-reconciliation-20260817.md, craq-lane-cleanup-20260817, lineage-hunt-20260817.md (a parallel F1-track 276-line rtl-rvtt-loadmacro.cc with `subunit_calendar_missing` exists in the F1 sfpi checkouts — different lineage, reconcile/retire), morning report /home/nkapre/MORNING_REPORT.md. Hybrid toolchains sdpa-gap-*-toolchain etc. (frozen provenance for the recorded hashes). Paused-lane worktrees sfpi-gcc-trackb / tt-metal-exp-attack / sfpi-gcc-sigappx (local-only, partial; §7 re-executes from remotes if lost). In-flight-at-rewrite: WP8 progress in sfpi-gcc-macro-planner and the unification worktree of sfpi-gcc-sfploadmacro — check `git log` there if the machine survives; otherwise §6/§8 restart from the remotes. Worktree triage 2026-08-17: 7 archived to tt-metal archive/* refs (tt-llk-audit-softmax-candidate, tt-metal-corpus-runner-v3, tt-metal-sfpi, tt-metal-sfpi-welford-{full-trace,microprobe}, tt-metal-sigappx-pwl, tt-metal-topk-f1), 32 deleted as superseded, 0 kept.
